# ngrok 인증 설정 가이드

## 🔐 ngrok 인증 토큰 설정 필요

ngrok을 사용하려면 무료 계정과 인증 토큰이 필요합니다.

## 📋 설정 단계

### 1. ngrok 계정 생성 (무료)

1. [ngrok 회원가입](https://dashboard.ngrok.com/signup) 페이지 접속
2. 이메일로 계정 생성 (무료)

### 2. 인증 토큰 받기

1. [ngrok 대시보드](https://dashboard.ngrok.com/get-started/your-authtoken) 접속
2. 로그인 후 "Your Authtoken" 섹션에서 토큰 복사

### 3. 인증 토큰 설정

터미널에서 다음 명령어 실행:

```bash
ngrok config add-authtoken YOUR_AUTHTOKEN_HERE
```

또는 수동으로 설정 파일 생성:

```bash
mkdir -p ~/.ngrok2
cat > ~/.ngrok2/ngrok.yml << EOF
authtoken: YOUR_AUTHTOKEN_HERE
EOF
```

### 4. 설정 확인

```bash
ngrok config check
```

## 🚀 설정 후 실행

인증 토큰 설정 후:

```bash
# 방법 1: 자동 스크립트 사용
./scripts/setup-ngrok.sh

# 방법 2: 수동 실행
ngrok http 3000
```

## 💡 대안: 로컬 테스트만 하는 경우

TradingView 연동 없이 로컬에서만 테스트하는 경우:

1. **로컬 웹훅 테스트**: `./scripts/test-webhook.sh` 사용
2. **프로덕션 배포**: Vercel에 배포하면 자동으로 공인 URL 제공

## 📝 참고

- ngrok 무료 플랜: 충분히 사용 가능
- 인증 토큰은 한 번만 설정하면 됩니다
- 설정 후에는 `ngrok http 3000` 명령어만으로 실행 가능

